const LoupGarou = require('../../../games/loup-garou/LoupGarou')

describe('loup-garou', () => {
  test('start game default', async () => {
    //
    // const assertGameStart = (event, state) => {
    //   expect.assertions(12)
    //   if (event === 'player join') expect(state.id && state.name).toBeTruthy()
    //   if (event === 'wait ready') {
    //     const players = state.players
    //     expect(players.filter(player => player.role === 'LG').length).toEqual(2)
    //     expect(players.filter(player => player.role === 'Sor').length).toEqual(1)
    //     expect(players.filter(player => player.role === 'Voy').length).toEqual(1)
    //     expect(players.filter(player => player.role === 'Vol').length).toEqual(1)
    //     expect(players.filter(player => player.role === 'Vil').length).toEqual(2)
    //   }
    // }
    //
    // const loupGarou = new LoupGarou(assertGameStart, {})
    // await loupGarou.next()
    //
    // await loupGarou.run('player join', { id: 1, name: 'one' })
    // await loupGarou.run('player join', { id: 2, name: 'two' })
    // await loupGarou.run('player join', { id: 3, name: 'three' })
    // await loupGarou.run('player join', { id: 4, name: 'four' })
    // await loupGarou.run('player join', { id: 5, name: 'five' })
    // await loupGarou.run('player join', { id: 6, name: 'six' })
    // await loupGarou.run('player join', { id: 7, name: 'seven' })
  })
})